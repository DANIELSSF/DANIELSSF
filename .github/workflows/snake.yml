name: Generate contribution snake

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate snake SVGs (dark and light, brand palette)
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#7dd3fc&color_dots=#161b22,#0c4a6e,#0369a1,#38bdf8,#7dd3fc
            dist/github-snake.svg?palette=github-light&color_snake=#0369a1&color_dots=#ebedf0,#bae6fd,#7dd3fc,#0ea5e9,#0369a1

      - name: Publish SVGs to the output branch
        uses: crazy-max/ghaction-github-pages@v4.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
